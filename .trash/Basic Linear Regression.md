
# Regression Problem

#### Given 
* observations $$\Large X= \{x_1, x_2, \dots, x_n \}, \quad x_i \in \mathbb{R}^D $$
* targets 
  $$\Large
  y = \{y_1, \dots, y_N\}, \quad y_i \in \mathbb{R}
  $$

#### Find 
Mapping $f(\cdot)$ from inputs to targets
$$\Large
y_i \approx f(x_i)
$$

---

## Linear Model

Target $y$ is generated by a deterministic function $f$ of $x$ plus noise $\varepsilon$ 

$$\Large
y_i = f(x_i) + \epsilon_i,
\quad \epsilon_i \sim \mathcal{N}(0, \beta^{-1})
$$

Choose linear function:

$$\Large
f_w(x_i) = w_0 + w_1 x_{i1} + \dots + w_D x_{iD}
$$

Compact form:

$$\Large
f_w(x_i) = w_0 + w^T x_i
$$
Where $\omega_0$ is called the bias or offset term. 

---
# Absorbing the Bias
For simplicity, we can "absorb" it by prepending a 1 to the feature vector $x$ and respectively adding $\omega_0$ to the weight vector $\mathbb{\omega}$:
$$
\Large
\tilde{x} = (1, x_1, \dots, x_D)^T \qquad
\tilde{w} = (w_0, w_1, \dots, w_D)^T
$$
The function $f_\omega$ can then completely be written as 
$$\Large
f_w(x) = \tilde{w}^T \tilde{x}
$$
From here on out this is presumed.

---
# Loss Function - Least Square

Now, how do we choose the "best" $\omega$ that fits our data?

A **loss function** measures the "misfit" or error between our model (parameterized by $\omega$) and observed data $D = \{(x_i, \ y_i)\}_{i=1}^N$.

Standard choice - **[[Minsta Kvadrat Metoden (MK)]] / Least Square (LS)** 
$$\Large
E_{LS} = 
\frac 1 2 \sum_{i=1}^{N}(f_w(x_i) - y_i)^2
$$

Matrix form:
$$
\Large 
E_{LS} = \frac 1 2 \sum_{i=1}^N (\omega^T x_i - y_i)^2
$$
![[Pasted image 20260503131613.png|682]]

---
# Objective

The objective of any Linear Regression problem is to find the optimal weighted vector $\omega^*$ that minimizes the error
$$\Large
w^* = \arg\min_w E_{LS}(\mathbb{\omega}) = 
\arg\min_\omega (x_i^T \omega - y_i)^2
$$
By stacking the observation $x_i$ as rows of the matrix $X \in \mathbb{R}^{N \times D}$ 
$$\Large
= \arg\min \frac 1 2 (X\omega - y)^T(X\omega - y)
$$
---
# Optimal Solution

To find the minimum of the loss $E(\omega)$, compute the [[Gradient]] $\nabla_\omega E(\omega)$:
$$\Large
\begin{array}a
\nabla_w E_{LS}(w) = \nabla_w \frac 12(Xw - y)^T(Xw - y)\\
= \nabla_w \frac 12(w^T X^T X w - 2 w^T X^T y + y^T y) \\
= X^T X w - X^T y
\end{array}
$$
Meaning we derive the function $E_{LS}$. Then we set it to zero:
$$\Large
X^TXw - X^T y = 0
$$
which leads to the **normal equation** of the least squares problem:
$$\Large
w^* =
\underbrace{(X^T X)^{-1} X^T}_{X^\dagger} y
=
X^\dagger y
$$
Where $X^\dagger$ is called the **[[Moore-Penrose pesudo-inverse]]**   

---
